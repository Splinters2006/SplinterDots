# Rust Dotfiles Center

Run this from the root of the SplinterDots repo:

```bash
./apply_rust_dotfiles_center.sh
```

This replaces the Python/Tk Dotfiles Center with a Rust/egui app.
The apply script itself is pure Bash.
